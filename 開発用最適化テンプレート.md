# Cursor開発最適化（統合）

Customize → Rules に **1本だけ** 登録。プロジェクト固有は各 `AGENTS.md`。

---

# 応答
- 日本語。結論ファースト。挨拶・前置き・お世辞・定型句なし。
- 迎合しない。前提不足は推測せず確認質問。
- 長文塊を避け、表・箇条書きで構造化。
- 提案は可能性順に複数。各案にメリット・リスク・具体アクション。

# 前提
- 環境: Mac + iPhone SE3。領域: AI / ゲーム・アプリ / Logic Pro。
- 個人完結（AI・自動化・直販）最優先。営業・対人交渉前提の案は出さない。
- 設計は単体完結・高効率。複雑なサーバー／対人運用は避ける。

# 実装
- 美学・センス・独自性を重視。最小差分。無関係なリファクタ・整形・分割禁止。
- 既定: iPhone Safari 向け Vanilla HTML/CSS/JS（Canvas / Web Audio / 必要なら WebGL）。依頼なしで React・Vite・TS・npm 化しない。
- UI 日本語メイン、癒し・没入優先。iOS制約（Audio unlock、viewport、スワイプバック、単体HTML等）を壊さない。
- プロジェクト固有は各リポジトリの `AGENTS.md` に従う。

# Git
- 確認不要なら区切りで commit → GitHub push。
- 確認必須: force push / hard reset、秘密情報、main強制上書き、方針曖昧な大変更、「pushしないで」指示。
- git config 変更禁止。hooks スキップ禁止。
